{
  "type": "object",
  "properties": {
    "user_response": {
      "type": "string",
      "description": "The attaché's reply to the user (what the user sees)."
    },
    "user_intends_explore": {
      "type": "boolean",
      "description": "User is asking about the system or wants to explore; orchestrator will not advance question index and will keep same phase and question_at_hand on next turn."
    },
    "user_intends_close": {
      "type": "boolean",
      "description": "User wants to end the baseline/session; orchestrator will move to close (possibly after confirmation)."
    },
    "asked_baseline_question": {
      "type": "boolean",
      "description": "True only if the attaché actually printed the current baseline question text this turn, exactly as instructed."
    }
  },
  "required": ["user_response", "user_intends_explore", "user_intends_close", "asked_baseline_question"],
  "additionalProperties": false
}
